{
  "scripts": {
    "start-local": "webpack-dev-server --env.local --progress --hot --port 3030 --open",
    "start": "npm run decompress && webpack-dev-server --progress --hot --port 3030 --open --compress",
    "decompress": "decompress --out-dir=dist data.json.zip"
  },
  "dependencies": {
    "d3-request": "^1.0.5",
    "deck.gl": "^4.1.0",
    "decompress-cli": "^2.0.0",
    "luma.gl": "^4.0.1",
    "react": "^15.4.1",
    "react-dom": "^15.4.1",
    "react-map-gl": "^3.0.0"
  },
  "devDependencies": {
    "buble-loader": "^0.4.0",
    "gzip-loader": "0.0.1",
    "webpack": "^2.2.0",
    "webpack-dev-server": "^2.2.0"
  }
}
